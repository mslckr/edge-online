import { ItemFilterNamePipe } from './item-filter-name.pipe';

describe('ItemFilterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemFilterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
