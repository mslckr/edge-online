import { ItemFilterStockPipe } from './item-filter-stock.pipe';

describe('ItemFilterStockPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemFilterStockPipe();
    expect(pipe).toBeTruthy();
  });
});
