import { ItemFilterProviderPipe } from './item-filter-provider.pipe';

describe('ItemFilterProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemFilterProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
