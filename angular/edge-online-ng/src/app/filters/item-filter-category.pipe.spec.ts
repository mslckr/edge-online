import { ItemFilterCategoryPipe } from './item-filter-category.pipe';

describe('ItemFilterCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemFilterCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
